<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:title="Recolecciones solicitadas"
        app:titleTextColor="@android:color/white" />

    <!-- Mapa y cuadro de distancias en FrameLayout -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <org.osmdroid.views.MapView
            android:id="@+id/mapSolicitadas"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Cuadro de distancias como CardView -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardDistancias"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_margin="16dp"
            android:visibility="gone"
            app:cardCornerRadius="12dp"
            app:cardElevation="8dp">

            <ScrollView
                android:layout_width="match_parent"
                android:layout_height="150dp"
                android:padding="8dp">

                <LinearLayout
                    android:id="@+id/layoutDistancias"
                    android:orientation="vertical"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />

            </ScrollView>
        </androidx.cardview.widget.CardView>

    </FrameLayout>

    <!-- Botón moderno y redondeado -->
    <Button
        android:id="@+id/btnMostrarDistancias"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mostrar Distancias"
        android:layout_margin="10dp"
        android:textColor="@android:color/white"
        android:padding="12dp"
        android:fontFamily="sans-serif-medium"
        android:layout_gravity="center"
        android:clipToOutline="true" />
</LinearLayout>
