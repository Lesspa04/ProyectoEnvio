<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoEnvio">

        <meta-data
            android:name="org.osmdroid.config"
            android:resource="@xml/osmdroid_config" />

        <!-- Nueva pantalla inicial: SplashActivity -->
        <activity
            android:name=".menus.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Resto de actividades -->
        <activity android:name=".loginSignup.RegisterActivity" android:exported="false" />
        <activity android:name=".loginSignup.LoginActivity" android:exported="false" />
        <activity android:name=".EnvioActivity" android:exported="false" />
        <activity android:name=".RutasActivity" android:exported="false" />
        <activity android:name=".MisRecoleccionesActivity" android:exported="false" />
        <activity android:name=".SeguimientoActivity" android:exported="false" />
        <activity android:name=".MapaDireccionActivity" android:exported="false"/>
        <activity android:name=".RecoleccionDetalleActivity" android:exported="false"/>
        <activity android:name=".perfil.PerfilActivity" android:exported="false"/>


        <!-- La que antes era MainActivity (ahora MenuPrincipalActivity) -->
        <activity android:name=".menus.MenuPrincipalActivity" android:exported="false" />

        <!-- Si mantenés MainActivity por ahora, sin usar -->
        <!-- <activity android:name=".MainActivity" android:exported="false" /> -->

    </application>

</manifest>
